#include <stdio.h>

int main() {
    float a, b;
    scanf("%f %f", &a, &b);
    float product;
    product = (int)a * (int)b;
    printf("Product: %d", product);
    printf("%d ")

    return 0;
}